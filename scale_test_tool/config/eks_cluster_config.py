# Amazon EKS Cluster level config parameters

# AWS region where the EKS cluster is created
REGION = '<AWS_REGION>'

# Replace <eks_cluster_name> with the name of the Amazon EKS cluster
# which will be used to run the scale test
EKS_CLUSTER_NAME = "<EKS_CUSTER_NAME>"
